services:
  bifrost:
    image: maximhq/bifrost:latest
    container_name: bifrost
    labels:
      yantr.app: "bifrost"
      yantr.service: "AI Gateway"
    ports:
      - "8080"
    volumes:
      - bifrost_data:/app/data
    restart: unless-stopped

volumes:
  bifrost_data: