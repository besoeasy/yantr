services:
  pyramid:
    image: ghcr.io/fiatjaf/pyramid:latest
    restart: unless-stopped
    ports:
      - "3334"
    volumes:
      - data:/app/data
    labels:
      yantr.app: "pyramid"
      yantr.service: "Nostr Service Suite"

volumes:
  data: {}