services:
  transmission:
    image: linuxserver/transmission:latest
    container_name: transmission
    environment:
      USER: ${TRANSMISSION_USER:-admin}
      PASS: ${TRANSMISSION_PASS:-admin}
    volumes:
      - transmission_config:/config
      - transmission_downloads:/downloads
    ports:
      - "9091"
      - "51413"
      - "51413/udp"
    restart: unless-stopped
    labels:
      yantr.name: "Transmission"
      yantr.logo: "QmYNJtjCxWhoKS78Nvjfm7fc9dqA49QiGHUQBryYSmwsdQ"
      yantr.category: "download,network"
      yantr.port: "9091 (HTTP - Web Interface)"
      yantr.description: "Fast, easy BitTorrent client with web interface"
      yantr.website: "https://transmissionbt.com/"

volumes:
  transmission_config:
    name: transmission_config
  transmission_downloads:
    name: transmission_downloads
