services:
  samba:
    image: dockurr/samba:latest
    container_name: samba
    environment:
      NAME: ${SAMBA_NAME:-Data}
      USER: ${SAMBA_USER:-admin}
      PASS: ${SAMBA_PASS:-admin}
    ports:
      - "445:445"
    volumes:
      - samba-storage:/storage
    restart: unless-stopped
    labels:
      yantr.name: "Samba"
      yantr.logo: "QmYNjTj44mLCz5SZrWR9GkWU94bZw7hvqZZuFAwJJcsWU7"
      yantr.category: "network,storage,utility"
      yantr.port: "445 (SMB - File Sharing)"
      yantr.description: "SMB/CIFS file sharing server for network storage access"
      yantr.website: "https://github.com/dockur/samba"

volumes:
  samba-storage:
    name: samba-storage
