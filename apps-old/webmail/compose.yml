x-yantr:
  name: "webmail"
  tags: [email, webmail, self-hosted, podman, jmap]
  short_description: "Modern JMAP webmail client for Stalwart mail server."
  description: >
    A lightweight, modern webmail client built specifically for Stalwart
    mail server via JMAP. Enjoy a fast, responsive interface with instant
    search, message threading, and low resource overhead on your homelab.
  usecases: ["Read email in browser.", "Manage mail folders.", "Compose and send messages."]
  website: "https://github.com/bulwarkmail/webmail"

services:
  webmail:
    image: ghcr.io/bulwarkmail/webmail:latest
    labels:
      yantr.app: "webmail"
      yantr.service.8080: "Web UI"
      yantr.port.8080: "HTTP"
    environment:
      JMAP_HOST: ${JMAP_HOST:-localhost}
      JMAP_PORT: ${JMAP_PORT:-443}
    ports:
      - "8080"
    volumes:
      - webmail_data:/data
    restart: unless-stopped

volumes:
  webmail_data:
