services:
  traggo:
    image: traggo/server:latest
    container_name: traggo
    labels:
      yantr.app: "traggo"
      yantr.service: "Time Tracker"
    ports:
      - "3030"
    environment:
      admin: ${admin}
      mynewpassword: ${mynewpassword}
    volumes:
      - traggo_data:/opt/traggo/data
    restart: unless-stopped

volumes:
  traggo_data:
